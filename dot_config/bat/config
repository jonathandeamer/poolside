# bat config — custom Poolside theme (see themes/Poolside.tmTheme).
# After changing the theme file, rebuild bat's cache: `bat cache --build`.
--theme="Poolside"
--style="numbers,changes,header"
--italic-text=always
--paging=auto
